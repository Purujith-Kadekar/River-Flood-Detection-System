# Raspberry Pi LoRa Gateway Dependencies
# Install with: pip3 install -r requirements.txt

# LoRaRF SX127x driver
LoRaRF>=1.4.0

# SPI support used by LoRaRF
spidev>=3.5

# Required for the Flask dashboard (app.py)
flask>=2.0.0
flask-sqlalchemy>=3.0.0

# Optional: SMS alerts (.env) and reading .env files
requests>=2.28.0
python-dotenv>=1.0.0